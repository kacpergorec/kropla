<!DOCTYPE html>
<html style="" lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Kropla Admin
        {% block title %}

        {% endblock %}
    </title>

    {% include 'base/encore.html.twig' %}
</head>
<body>
<div class="h-screen flex overflow-hidden">

    <aside class="min-w-[250px] bg-slate-800 relative h-full z-30 text-white p-6 flex flex-col">
        <a href="{{ path('admin_panel') }}" class="font-semibold">
            <img src="{{ asset('build/images/logo/wide-transparent-darkmode.webp') }}"
                 alt="Kropla logo" class="logo inline -mt-3 mr-2"/>
            Admin
        </a>
        <nav class="grow overflow-y-auto mt-8">
            {% include 'admin/components/menu.html.twig' %}
        </nav>
    </aside>

    <div class="flex flex-col overflow-y-auto relative z-0 grow">

        {% include 'admin/components/header.html.twig' %}

        <main class="bg-slate-100 p-12 py-8 grow">
            {% block body %}
                <p>
                    Hejka, {{ app.user.firstName }}! 👋
                </p>
                <p>
                    Zobacz co słychać na twoim blogu:
                </p>
            {% endblock %}
        </main>
    </div>

</div>
</body>
</html>